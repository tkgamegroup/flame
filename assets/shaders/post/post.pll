//#include "../dynamic_data.dsl"

layout (set = SET, binding = 0) uniform sampler2D img_src;

layout (push_constant) uniform PushConstant
{
	int off;
	int len;
	float weights[14];
	vec2 pxsz;
	float white_point;
	float one_over_gamma;
}pc;
