file(GLOB_RECURSE SOURCES "*.h*" "*.c*")
add_executable(shader_info ${SOURCES})
set_target_properties(shader_info PROPERTIES FOLDER "tools")
target_include_directories(shader_info PRIVATE "${SPIRV_CROSS_INCLUDE_DIR}")
target_link_libraries(shader_info flame_foundation)
target_link_libraries(shader_info "${SPIRV_CROSS_LIBRARY_DIR}/spirv-cross-cored.lib")
target_link_libraries(shader_info "${SPIRV_CROSS_LIBRARY_DIR}/spirv-cross-reflectd.lib")
target_link_libraries(shader_info "${SPIRV_CROSS_LIBRARY_DIR}/spirv-cross-cppd.lib")
target_link_libraries(shader_info "${SPIRV_CROSS_LIBRARY_DIR}/spirv-cross-glsld.lib")
