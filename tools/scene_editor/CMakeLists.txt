generate_config()
file(GLOB_RECURSE source_files "*.h" "*.c*")
add_executable(scene_editor ${source_files})
set_target_properties(scene_editor PROPERTIES FOLDER "tools")
target_link_options(scene_editor PRIVATE /FIXED:NO)
target_link_libraries(scene_editor flame_universe)
set_target_properties(scene_editor PROPERTIES VS_DEBUGGER_WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}")

if(USE_FORTUNE_ALGORITHM)
	target_compile_definitions(scene_editor PUBLIC USE_FORTUNE_ALGORITHM)
	target_include_directories(scene_editor PRIVATE "${FORTUNE_ALGORITHM_DIR}/src")
	target_link_libraries(scene_editor "${FORTUNE_ALGORITHM_DIR}/build/Debug/Fortune.lib")
endif()
